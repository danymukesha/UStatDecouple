# Create U-statistic kernel

Create U-statistic kernel

## Usage

``` r
create_kernel(kernel_function, kernel_name, symmetric = TRUE)
```

## Arguments

- kernel_function:

  Function taking two arguments

- kernel_name:

  Character name for the kernel

- symmetric:

  Logical indicating if kernel is symmetric

## Value

UStatKernel object
